Run the project's lint and test suite to verify your changes. Look for:
- A `Makefile` with `check`, `test`, `lint`, or `ci` targets
- `package.json` scripts (`npm test`, `npm run lint`)
- CI config (`.github/workflows/`) to see what the project runs

Fix any failures before committing.
