# PGAUTH-001 — Durable Macro-State Authority Contract

Task: `PGAUTH-001` ("Durable macro-state authority contract bounded") — parent `POSTGRES-001`, phase `2-postgres-foundation`.
Status: **owner-ratified target contract; implementation incomplete**. `STORE-001` revision 2 fixes the authority assignments below against both immutable architecture anchors. This contract does not claim that target schemas, bridges, recovery, or cutover are already deployed; current durable redb, SQLite, filesystem, and proof-ledger uses remain transitional until their named proof gates pass.

Scope: durable *editable* macro-state — stable identities, histories, provenance, permissions, blob references, relations, and semantic vectors — per the PGAUTH-001 goal. Transient/local redb state is inventoried separately in `generated/pgauth_redb_usage_inventory.csv` (PGAUTH-002); cognition state in PGAUTH-003; projections in PGAUTH-004; external sources in PGAUTH-005.

Evidence base: the exact-byte receipts and section locations in `1.0_VISION/Architecture_Anchors/receipts.json` and `section_inventory.json`, the controlling resolutions in `anchor_conflict_ledger.csv`, `STORE-001` revision 2, and the current-state claim evidence in `generated/notebooklm_source_claims.source.csv`, `NBVERIFY-001.truth-matrix.csv`, and `NBVERIFY-003.truth-matrix.csv`. Anchor requirements define the target; checked implementation and proof still define what exists now.

## Authority table — durable state families

| State family | Target owner and current boundary | Writers | Readers | Sync path | Failure mode / recovery | Evidence citations |
|---|---|---|---|---|---|---|
| Source-code truth and all captured host bytes | **PostgreSQL + RuVector target.** Current CodeDB redb durability is transitional; the implemented `codedb_store_pg::PgStore`/`BlobStore` parity seam is a migration capability, not cutover proof. | CodeDB typed ingestion through envctl-governed commit; redb owner is pass-through only | Materializers, bounded readers, audit | raw bytes are teed before transformation → typed envelope → redb owner spool → envctl commit → PostgreSQL; database-controlled state projects back through CodeDB/envctl. No automated end-to-end bridge exists today. | Current redb recovery remains valid before cutover. Target recovery is WAL/PITR plus byte/metadata/provenance reconstruction under `ARCHBP-038` and `ARCHBP-045`. | ARCHANCHOR-001 hard rules 1–12 and §§3–4, 16, 19; ARCHANCHOR-002 invariants 1, 7–10; REDB-CLAIM-001/004; NUPG-CLAIM-005/006 |
| Migration/orchestration and operational work history | **PostgreSQL + RuVector target.** Current envctl `migration_db` redb tables remain transitional runtime state. | envctl is the exclusive production PostgreSQL committer; database-governed jobs control post-cutover operations | envctl CLI/GUI, agents, audit | ordered redb owner records → envctl validate/embed/commit → PostgreSQL; committed decisions and results return through controlled projections. | Current envctl replay remains valid; target recovery is PostgreSQL PITR plus exact effect/result/witness receipts. | ARCHANCHOR-001 §§3.2, 4.7–4.8, 13, 18–20; ARCHANCHOR-002 §§6, 8, 12–14; ARCHBP-042/046 |
| Stable identities & permissions | **PostgreSQL + RuVector target.** Current LifeOS SQLite accounts/roles and keyring integrations are explicitly transitional, not a second target authority. | LifeOS runtime through governed commands and least-privilege DB roles | Scoped agents and UI | owner-approved, exact per-class migration; no dual-write cutover. | Current `db_health`/`db_migrate`; target WAL/PITR and reconstruction proof. | STORE-001 revision 2; ARCHBP-038/044/045 |
| Provenance chains & version history | **PostgreSQL + RuVector target** for complete inputs, intermediates, effects, results, conflicts, and witnesses. The file proof ledger remains the repository-native operating authority until cutover. | Verifier/proof pipeline and governed database jobs | Agents, audit, release gates | Append-only accepted evidence; projections never write back. | Current file ledger plus Git history; target PITR and witness reconstruction. | ARCHANCHOR-001 §§17–20; ARCHANCHOR-002 invariants 7–10; ARCHBP-045/046/047 |
| Relational metadata (task graphs, object rows, planning tables) | **PostgreSQL + RuVector target.** Authored CSV/JSON inputs remain canonical repository-native planning inputs until `ARCHBP-046` and final cutover are proven. | Planning generators now; database-governed job APIs after cutover | All agents | maintained source → deterministic projection now; database tables → deterministic projections after cutover. | Git and source inventories now; PostgreSQL recovery after cutover. | STORE-001 revision 2; ARCHBP-046/048 |
| Semantic vectors & embeddings | **PostgreSQL + RuVector target.** Vectors and indexes are derived from retained original bytes/typed rows and never replace them. | envctl-orchestrated approved embedding path | Semantic search consumers | versioned source → model/config-bound embedding → RuVector row/index; return projection through envctl/CodeDB. | Rebuild indexes from retained source bytes and model identity. Live extension activation is still unproven. | ARCHANCHOR-001 §§3.2, 4.7, 16; REDB-CLAIM-023; ARCHBP-042/045 |
| Large binary artifacts and protected encrypted secret bytes | **PostgreSQL + RuVector target stores the bytes and metadata**, not merely references. Plaintext secrets are excluded; protected encrypted bytes, custody, leases, rotation, and audit are durable. | Capture pipeline or six-part secret lifecycle through governed envctl roles | Authorized materializers or lease-bound target only | bytes retained beside derivatives; secret ciphertext enters custody, plaintext is released only to the exact authorized target for the lease lifetime. | PITR plus byte reconstruction; secret recovery also requires owner-controlled root material. | ARCHANCHOR-001 hard rules 11–16, §5, §16; ARCHANCHOR-002 §9; ARCHBP-038/044/045 |

## Contract invariants (apply to every row)

1. **Exactly one canonical owner per durable state class.** Anything else is a derived copy with a declared, directional, idempotent sync edge (POSTGRES-001 gate).
2. **RuVector-or-any-index is derived, never authoritative.** It must be rebuildable from versioned source rows alone.
3. **No dual-write.** A writer writes to the canonical store; replicas/copies are filled only by the declared sync path.
4. **No silent cutover.** `STORE-001` assigns the target, but current durable redb, SQLite, filesystem, and proof-ledger uses remain operational until byte parity, recovery, and owner-approved per-class cutover proof passes.
5. **Refuted mechanisms may not justify assignments.** The mmap/OS-cache-bypass and AVX-512 arguments for redb are refuted (REDB-CLAIM-002, REDB-CLAIM-010, EDGE-CLAIM-002); durable-authority arguments must rest on the verified benchmark (`NBVERIFY-001.redb-benchmark.summary.json`) and the parity-tested implementations.

## Open items blocking closure

- `ARCHBP-038` byte-complete import and reconstruction, including current redb/SQLite/file classes.
- `ARCHBP-042` envctl-only production commit and bidirectional reconciliation.
- `ARCHBP-044` protected secret custody and six-part lifecycle.
- `ARCHBP-045` WAL/replication/PITR and zero-loss reconstruction.
- `ARCHBP-046..048` database-controlled work, conformance, and final owner-approved cutover.
- Local verification of PostgreSQL durability claims (RUVPG-CLAIM-005) and RuVector extension activation (REDB-CLAIM-023 remainder).
