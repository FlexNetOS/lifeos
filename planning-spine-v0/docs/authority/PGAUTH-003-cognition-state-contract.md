# PGAUTH-003 — Per-Agent Cognition Authority Contract (DRAFT)

Task: `PGAUTH-003` ("Per-agent cognition authority contract bounded") — parent `PGAUTH-001`, phase `2-postgres-foundation`.
Status: **draft, planning-only**. Every owner-store assignment below is a *proposed* assignment and remains **pending STORE-001** (the David-approved store-relationship decision). No agent is run, no model is trained, no `.rvf` file is written, and no cognition state is promoted to shared truth by this document.

Scope: cognitive state that is canonical **only for one agent** — learning traces, trajectories, local adaptations (MicroLoRA weights), routing memory (FastGRNN), and witness-bound memory — per the PGAUTH-003 goal. PostgreSQL + RuVector retains shared macro-state authority (PGAUTH-001); this contract draws the boundary that keeps AgentDB from silently owning shared user-space truth.

Evidence base: `generated/notebooklm_source_claims.source.csv` (claim map), `generated/notebooklm_claim_verification/NBVERIFY-001.truth-matrix.csv` (the only matrix that ran a live AgentDB/SONA probe — rows REDB-CLAIM-027 and REDB-CLAIM-028), and `generated/notebooklm_claim_evidence.source.csv`. Citations below are claim IDs; their verification statuses are those recorded in the matrices, not restatements of NotebookLM prose. Every AGENTDB-/COGEVO-/RUVCOG- claim about self-learning behaviour is `mapped-verification-required` (proposal, not proven current design) unless a live-probe row upgrades it.

## Authority table — cognitive state families

| State family | Owner store (proposed) | Writers | Readers | Sync path | Failure mode / recovery | Evidence citations |
|---|---|---|---|---|---|---|
| Learning traces & trajectories (per-agent success/failure history, SONA feedback) | **AgentDB `.rvf`, per agent (proposed).** Pending STORE-001. | The owning agent's SONA runtime only (single-writer per `.rvf`) | The owning agent; nothing else reads another agent's raw traces | **One-way promotion only**, agent trace → curated summary → PostgreSQL shared macro-history via a governed command. Raw traces never sync into shared state. | `.rvf` is a single portable file; loss degrades one agent's adaptation, not shared truth. **Known defect:** the live Bun probe found `recordFeedback` recorded **zero** trajectories via the documented hit-ID call (REDB-CLAIM-027 qualified) — the feedback-to-ranking loop is currently defective, so trace durability cannot yet be relied on. | REDB-CLAIM-027 (qualified — `SelfLearningRvfBackend` over `.rvf` proven; feedback contract defective), COGEVO-CLAIM-010 (mapped-verification-required), AGENTDB-CLAIM-006, AGENTDB-CLAIM-007, RUVCOG-CLAIM-003, RUVCOG-CLAIM-004 |
| Local adaptations (MicroLoRA adapter weights = per-agent skills/personality) | **AgentDB `.rvf`, per agent (proposed).** Pending STORE-001. | Agent training/adaptation runtime (single-writer) | Agent runtime at inference; hot-swap loader | Adapters stay agent-local. A promoted skill becomes a **new shared artifact** written to macro-state by a governed command — never a back-write into another agent's `.rvf`. | Rebuild from base model + retained trace summaries. **Not proven:** no single current `.rvf` was shown to persist both MicroLoRA adapters and a FastGRNN model together, nor 50+ personalities within one measured footprint (REDB-CLAIM-028 qualified; AGENTDB-CLAIM-009 mapped-verification-required). | REDB-CLAIM-028 (qualified — RVF/SONA/ruvllm/Tiny-Dancer components proven separately, not jointly in one file), AGENTDB-CLAIM-004, AGENTDB-CLAIM-005, RUVCOG-CLAIM-005 |
| Routing memory (FastGRNN pre-filter / attention map) | **AgentDB `.rvf`, per agent (proposed).** Pending STORE-001. | Agent routing trainer (single-writer) | Agent's pre-LLM context selector | Derived from the agent's own traces; disposable and re-trainable. Never authoritative over shared retrieval. | Re-train from traces. Proposal only — no current LifeOS FastGRNN routing deployment is proven. | AGENTDB-CLAIM-008, RUVCOG-CLAIM-006, RUVCOG-CLAIM-007, RUVCOG-CLAIM-008 (all mapped-verification-required) |
| Witness-bound memory (claim ↔ source-vector provenance links) | **Shared-state boundary object.** The *link* is proposed to live in PostgreSQL macro-history (PGAUTH-001 provenance row); the agent holds only a reference. Pending STORE-001. | Verifier/proof pipeline (not the agent) | Agent (read reference), audit | Agent emits a claim + candidate source vector → verifier binds it → link stored in shared provenance. One-way out of cognition. | No witness-chain / SHAKE256 binding is implemented anywhere; today the operating provenance store is file-based `proof_records/proof_ledger.jsonl`. | WITNESS-CLAIM-004, WITNESS-CLAIM-005, WITNESS-CLAIM-006, WITNESS-CLAIM-007, RUVCOG-CLAIM-009, RUVCOG-CLAIM-011 (all mapped-verification-required — unimplemented) |
| Deployment container identity (which `.rvf` = which agent, base-model binding) | **AgentDB `.rvf` + PostgreSQL registry row (proposed split):** bytes in `.rvf`, identity/registration row in macro-state. Pending STORE-001. | Agent lifecycle manager | Orchestrator, UI | Registry row references the `.rvf` by content hash; `.rvf` never registers itself as shared authority. | Re-register from file; orphan `.rvf` without a registry row is quarantined, never auto-adopted. | AGENTDB-CLAIM-001, AGENTDB-CLAIM-002, AGENTDB-CLAIM-003, COGEVO-CLAIM-005, COGEVO-CLAIM-006 (all mapped-verification-required) |

## Contract invariants (apply to every row)

1. **Cognition is per-agent and non-authoritative over shared truth.** An `.rvf` may be canonical for its own agent's learning; it may never be the canonical source of any shared macro-state class (PGAUTH-003 gate). The redb-passive-vs-AgentDB-active framing (COGEVO-CLAIM-001, COGEVO-CLAIM-011, authority-proposal, unverified) is recorded as a *proposal*, not an authority grant.
2. **Promotion is explicit, one-way, and governed.** Anything moving from cognition into shared macro-state passes through a named command that produces a new shared artifact with its own identity; there is no implicit merge and no back-write into another agent's cognition.
3. **Portability + retention are declared per class.** Each row states whether it is disposable (routing, adapters — re-derivable) or retained (curated trace summaries). Raw traces are agent-private and never bulk-synced.
4. **Proven-component ≠ proven-system.** REDB-CLAIM-027/028 prove the *pieces* exist and load, but the qualified verdicts (defective feedback loop; components never shown jointly in one file) forbid treating the full self-learning cognitive-container story as current fact.
5. **No secret or shared-user-space value enters an `.rvf` as authority.** Cognition holds references and learned weights, not canonical user records.

## Open items blocking closure

- STORE-001 owner decision (assigns the AgentDB `.rvf` role and the cognition↔macro-state promotion boundary).
- Fix or formally accept the defective `recordFeedback` trajectory-ID contract before any trace is treated as durable (REDB-CLAIM-027 remainder).
- Local verification that one `.rvf` can jointly persist adapters + routing within a bounded footprint (REDB-CLAIM-028 remainder) — required before AGENTDB-CLAIM-009's 50-agents-one-footprint economics can be relied on.
- Witness-chain implementation does not exist; the provenance-link row is aspirational until built.
