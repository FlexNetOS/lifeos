# PGAUTH-004 — Derived Projection Authority Contract (DRAFT)

Task: `PGAUTH-004` ("Derived projection authority contract bounded") — parent `PGAUTH-001`, phase `2-postgres-foundation`.
Status: **draft, planning-only**. Every owner-store assignment below is a *proposed* assignment and remains **pending STORE-001** (the David-approved store-relationship decision). Nothing is materialized, no generated output is edited, and no projection is promoted to a writable source by this document.

Scope: derived projections — flat files, generated directories, build inputs, runtime materializations, reports, exports, and caches — declared as **disposable or preserved projections with one-way provenance** from canonical state, per the PGAUTH-004 goal. A projection may be regenerated or (if preserved) checksummed and retained, but it can never become a competing writable owner of the state it was projected from.

Evidence base: `generated/notebooklm_source_claims.source.csv` (claim map) and `generated/notebooklm_claim_verification/NBVERIFY-001.truth-matrix.csv` + `NBVERIFY-003.truth-matrix.csv` (claim-scoped verification). **Reality check that shapes this whole contract:** the NotebookLM "envctl projection engine from database to directory" narrative is largely *refuted* against current code — envctl does **not** query PostgreSQL for a final branch, does **not** reconstruct directories from `module_path`, and does **not** concatenate `raw_code` into files (REDB-CLAIM-024, ENVPROJ-CLAIM-002/003/004, MAT-CLAIM-003/004/005, all refuted). Current materialization is a **separate CodeDB operation**. This contract therefore describes projections against *actual* materializers, not the source's proposed one.

## Authority table — projected state families

| State family | Owner store / source of truth (proposed) | Writers (materializer) | Readers | Sync path (one-way provenance) | Failure mode / recovery | Evidence citations |
|---|---|---|---|---|---|---|
| Materialized source directories (byte-exact file trees rebuilt from stored blobs) | **Projection of the durable source-blob store** (PGAUTH-001 source-code-truth row; redb today, PG-or-redb pending STORE-001). Disposable. | `codedb` materialize (single deterministic materializer, SHA-256 verified) | Build environments (Nix/Yazelix), editors, agents | canonical blob rows → `codedb` materialize → files. **Edits to materialized files are not authoritative**; they must be re-captured through the capture CLI to reach canonical state. No PG-branch-selection materializer exists (REDB-CLAIM-024 refuted). | Re-materialize from blobs; verify each file by SHA-256 (`codedb_store_redb::checksum_file_sha256` pattern). Byte-exactness is the recovery guarantee. | REDB-CLAIM-011 (verified — store returns supplied bytes), REDB-CLAIM-024 (refuted — no PG-state→file projection today), MAT-CLAIM-001, MAT-CLAIM-004, MAT-CLAIM-005 (mapped-verification-required, source proposal), NUPG-CLAIM-005 (qualified) |
| Build inputs / portable-runtime closure (compiled directory handed to Nix/Yazelix) | **Projection of materialized directories.** Disposable rebuild artifact. Pending STORE-001. | Nix/Yazelix build (from an already-materialized tree) | Release pipeline, runtime | materialized dir → build env → runtime closure. **Not proven end-to-end:** no current selected-PG-state → materialized dir → isolated build trace exists (REDB-CLAIM-025 qualified). | Rebuild from materialized dir; the dir itself rebuilds from blobs. Portability is a whole-closure property, unproven for the current glibc/Nix-store-linked binaries (REDB-CLAIM-007 qualified). | REDB-CLAIM-025 (qualified), REDB-CLAIM-007 (qualified), MAT-CLAIM-006, MAT-CLAIM-007 (mapped-verification-required), ENVPROJ-CLAIM-005 (qualified) |
| Semantic export projections (JSONL rows carrying pre-computed embeddings, consumed by envctl) | **Projection/export of source + embeddings** (PGAUTH-001 semantic-vectors row). Disposable; re-exportable. Pending STORE-001. | Export pipeline (one writer) | envctl semantic adapter (builds an **in-memory** RuVector index — default off) | canonical source/vector rows → export JSONL → in-memory index. **Refuted that envctl writes back:** envctl reads exported rows and does not embed, pull redb blocks, or commit to PostgreSQL (REDB-CLAIM-021, INGEST-CLAIM-003, NUPG-CLAIM-008 refuted). One-way, read-only downstream. | Re-export from canonical rows; the in-memory index is fully disposable. | REDB-CLAIM-021 (refuted), INGEST-CLAIM-002 (refuted), INGEST-CLAIM-003 (refuted), NUPG-CLAIM-008 (refuted), ENVPROJ-CLAIM-001 (qualified) |
| Generated planning artifacts (`generated/**` CSV/JSON, status projections, reports) | **Projection of canonical planning tables.** Today the CSV/JSON *are* the operating source (git-tracked) until POSTGRES-003 defines a DB owner; post-cutover they become projections. Preserved (checksummed), not disposable. Pending STORE-001. | Planning scripts only (`scripts/*.py`) | All agents, gates, bundle packager | canonical table → deterministic script → generated file with checksum. Hand-editing a generated projection is forbidden once a DB owner exists. | Regenerate from source; `dist/*.SHA256SUMS` + git history are the recovery baseline. | REDB-CLAIM-013 (qualified — relational/SQL work is not redb's), MAT-CLAIM-008, DBX-CLAIM-013 (mapped-owner-decision-required — materialization is the final phase) |
| Caches (embedding cache, model-inference cache, build cache) | **Disposable projection of canonical inputs.** Keyed by content hash. Pending STORE-001. | Whichever pipeline populates the cache | Same pipeline on cache-hit | source input (content-hash key) → cache entry. A cache miss is always safe: recompute. Cache is never read as authority. | Delete and recompute. redb *can* back such a cache (feasible mechanism) but no current model-aware cache schema covering content/model/version/invalidation/collision is proven (REDB-CLAIM-016 qualified). | REDB-CLAIM-016 (qualified), REDB-CLAIM-011 (verified) |
| Reports & exports (dashboards, heartbeat, priority reports, DTCG/tailwind token exports) | **Disposable projection of live state.** Regenerated on demand. Pending STORE-001. | Report generators | Humans, UI | live state → generator → report file. Read-only artifact; never edited back into state. | Regenerate. Byte-deterministic exports (e.g. design tokens) are their own checksum. | MAT-CLAIM-002, MAT-CLAIM-009 (mapped-verification-required) |

## Contract invariants (apply to every row)

1. **One canonical source per projection; provenance is one-way.** Every projected class names its canonical source identity, the selected branch/version, and its deterministic materializer (PGAUTH-004 gate). Output → source back-writes are prohibited.
2. **Projection output can never become a competing writable owner.** Editing a materialized file, a generated CSV, or an export is not a write to canonical state; it reaches truth only by re-capture/ingestion through the canonical writer.
3. **Disposable vs preserved is declared, with a cleanup rule.** Caches/materialized dirs are disposable (safe to delete + regenerate); generated planning artifacts and deterministic exports are preserved with checksums.
4. **Checksum + metadata contract on every materialization.** SHA-256 byte-exactness (source dirs, blobs) or byte-deterministic regeneration (exports) is the integrity and recovery guarantee.
5. **Describe the real materializer, not the proposed one.** The refuted envctl-from-PostgreSQL projection story (REDB-CLAIM-024, ENVPROJ-CLAIM-002/003/004, MAT-CLAIM-003/004/005) may not be cited as current behaviour; only the actual `codedb` materialize path and the read-only envctl export consumer are current fact.

## Open items blocking closure

- STORE-001 owner decision (fixes which store is the canonical source behind the source-directory and semantic-export projections).
- POSTGRES-003/010 authority cutover (the point at which `generated/**` files stop being canonical and become projections).
- Local verification of the end-to-end selected-state → materialized dir → isolated build trace (REDB-CLAIM-025 remainder) and of portable-closure claims (REDB-CLAIM-007 remainder).
