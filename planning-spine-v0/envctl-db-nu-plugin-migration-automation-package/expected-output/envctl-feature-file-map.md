# Expected envctl feature file map

Codex must adapt paths to the actual repo. Expected categories:

```text
database migrations/schema
shared migration types/schemas
target descriptor parser
artifact contract registry
migration recipe loader
run manager
operation/event store
approval service
artifact/evidence store
package adapter
replay verifier
rollback/checkpoint metadata
CLI/API commands
tests
docs/issues
```
