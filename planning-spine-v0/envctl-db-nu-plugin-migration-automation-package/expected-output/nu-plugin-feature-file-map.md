# Expected nu_plugin feature file map

Codex must adapt paths to the actual repo. Expected categories:

```text
plugin command registration
migration status command
migration events/timeline command
migration operations command
migration approvals/approve/deny commands
migration artifacts command
migration graph command
migration validations command
migration replay command
shared protocol types
structured error handling
tests
docs/issues
```
