
# History

This folder is additive. It preserves the pre-upgrade package manifest before the execution-framework layer was added.

No source package file was overwritten to create the execution framework. Generated files and reports live under `execution-framework/` and `execution-templates/`.
