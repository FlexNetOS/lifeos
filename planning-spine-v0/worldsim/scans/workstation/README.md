# Workstation scan drop-zone

Reserved for an optional LiDAR scan of the physical workstation. The
workstation twin is already modeled from published manufacturer specs
(`../../specs/workstation_spec.json` — 15 components from the owner
Hardware Catalog), so a scan here is not required. Drop one only to
refine component placement against the real desk layout.
