<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-05-24 | Updated: 2026-05-24 -->

# src-tauri

## Purpose
Tauri 2 native shell. Wraps the Vite-built `../dist` bundle in a 1280×800 dark window, exposes a small set of `#[tauri::command]` functions for vault stubs, UI/lights state persistence, AI provider routing, system telemetry, and app metadata. Builds .deb / .AppImage / .dmg / .msi / .nsis / .rpm bundles.

## Key Files
| File | Description |
|------|-------------|
| `Cargo.toml` | Crate metadata + `tauri-build` + `tauri@2` + `tauri-plugin-fs@2` + `tauri-plugin-shell@2` + `reqwest@0.12` (rustls-tls — **no openssl-sys**) + `keyring@3` + `sysinfo@0.32` (system + network features only). Library name `lifeos_lib` (`staticlib`, `cdylib`, `rlib`). |
| `tauri.conf.json` | Identifier `ai.lifeos.desktop`. Window 1280×800, min 960×640, dark theme, background `#0A0A0A`. CSP allows Google Fonts + IPC. Bundle targets all desktop installers. `frontendDist: "../dist"`. `devUrl: "http://localhost:1420"` — strict port (Tauri expects it). `beforeDevCommand: "bun run dev"`, `beforeBuildCommand: "bun run build"`. |
| `build.rs` | Single call to `tauri_build::build()`. |
| `Cargo.lock` | Committed (Tauri convention). |

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `src/` | Rust source — `main.rs` (one line) + `lib.rs` (all logic) (see `src/AGENTS.md`). |
| `capabilities/` | Tauri 2 capabilities JSON — fs/shell/window/menu permissions (see `capabilities/AGENTS.md`). |
| `gen/` | **Generated by tauri-build.** JSON-schema files for capabilities validation. Do not hand-edit. |
| `icons/` | Full Tauri icon set (32px / 128px / @2x / .icns / .ico / Android mipmaps / iOS). Source: `../public/lifeos-app-icon.png`. Regenerate via `bun run tauri:icons` (run from `../`) when the source changes. |
| `target/` | Cargo build output. Always gitignored. |

## For AI Agents

### Working In This Directory
- **Never pull in `openssl-sys`.** All HTTP must stay on `reqwest` with `rustls-tls`. The host already has webkit2gtk-4.1 / libsoup-3.0 / gtk-3 / libxdo / ayatana-appindicator3 / librsvg2 / build-essential installed (Linux).
- FS scope is intentionally narrow. The capability JSON enables `fs:default` but `tauri.conf.json → plugins.fs.requireLiteralLeadingDot: false` is the only relaxation. Any directory access must be requested through a `#[tauri::command]` that builds its path off `app.path().app_data_dir()` — never accept a raw path from the frontend.
- Every new `#[tauri::command]` must be added to the `invoke_handler!` registration list in `src/lib.rs → run()` AND have a matching `invoke("...")` call (typically guarded by `tauriInvoke()`) in the frontend.
- Tauri builds are slow. Run `bun run tauri:build` only on demand, never as a routine verification step.

### Verification
```bash
cargo check                # quick syntax/type check inside src-tauri
bun run tauri:dev          # boot the dev shell from the repo root
bun run tauri:build        # native installer — slow, on demand only
```

## Dependencies

### Internal
- `../dist/` — frontend bundle (produced by `bun run build`).
- `../public/lifeos-app-icon.png` — icon source for `icons/`.

### External
- `tauri@2`, `tauri-plugin-fs@2`, `tauri-plugin-shell@2`
- `reqwest@0.12` (rustls-tls + json), `keyring@3`, `sysinfo@0.32`
- `serde@1`, `serde_json@1`

<!-- MANUAL: Add notes below; this section is preserved on regeneration. -->

`cargo tauri icon` 2.11 emits identical ICNS chunks in nondeterministic order.
After regenerating from the repo root, canonicalize and verify the container:

```bash
cargo tauri icon public/lifeos-app-icon.png
python3 scripts/canonicalize-icns.py src-tauri/icons/icon.icns
python3 scripts/canonicalize-icns.py --check src-tauri/icons/icon.icns
```
