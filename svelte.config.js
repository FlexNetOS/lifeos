// LifeOS — Svelte config (Glass shell).
// Components are plain JS + scoped CSS — no preprocessing needed. The file
// exists so @sveltejs/vite-plugin-svelte and svelte-check share one explicit
// config instead of warning about defaults.
export default {};
